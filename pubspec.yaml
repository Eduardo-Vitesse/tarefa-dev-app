name: tarefa_dev_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_masked_text2: ^0.9.1
  hive:
  hive_flutter:
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator:
  build_runner:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
